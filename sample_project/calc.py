def hello(name: str) -> str:
    """Say hello."""
    return f"Hello {name}"

class Calculator:
    """A simple calculator."""

    def multiply(self, x: float, y: float) -> float:
        """Multiply two numbers."""
        return x * y
